<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zaebi_pas_2020">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zaebi_PAS_2020">
        <activity android:name=".ItemDetailFavorite"></activity>
        <activity android:name=".ItemDetail" />
        <activity android:name=".DataAdapter"
            tools:ignore="Instantiatable" />
        <activity android:name=".ListDataFav" />
        <activity android:name=".ListData" />
        <activity android:name=".ControlClass" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Login" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>